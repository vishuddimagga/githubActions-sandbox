name: This is the name
run-name: This is the run-name
on: [fork, workflow_cal]
inputs:
  test:
    required: false
    type: string
jobs:
  JOB1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.test }}"